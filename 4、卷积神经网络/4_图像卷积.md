核函数是2维的，这是因为去掉了通道层（即R、G、B层），另外只有一个卷积核，所以核函数当前是2维的。

例子中首先按照6.2.2的公式，计算了输出通道的形状：即Y。之后按照Y进行互相关计算，得到输出通道的值。

感受野：
这个挺有意思，增加感受野的一个手段是增加输入特征的范围，另一种方式是增加网络的深度（增加卷积层的数量）（这个更优）。
当一个特征图中的任意元素需要检测更广区域的输入特征时，我们可以构建一个更深的网络。