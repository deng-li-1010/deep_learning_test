线上结论：卷积输出的形状有公式，因此卷积输出的形状取决于输入的形状和卷积核的形状。

提出一个问题：多层卷积后，得到的输出远小于输入大小。原因是卷积核高宽大于1导致，这么说也比较容易理解，如果卷积核是1*1，那么卷积层的输出形状和输入形状相同，输出与输入是一一对应的，形状不变。

如何解决：在输入图像的边界填充元素（通常填充元素是0）

如何填充：与卷积核的高、宽的奇偶相关，奇数时，上下左右都填充二分之一的（卷积核高度-1）的0。


步幅：
步幅也会影响输出形状。
